# dotfiles
my personal dotfiles

### vs code
just copy paste the jsons

### zsh config
If this repository resides in the /Users/Shared/ directory and you are in your user directory, execute:
```bash
echo "source /Users/Shared/dotfiles/.zshrc" > .zshrc
ln -s /Users/Shared/dotfiles/.aliases .aliases
```
install required plugins
